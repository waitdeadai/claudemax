node_modules/
dist/
*.tsbuildinfo
.DS_Store
.env
.env.local
.env.*.local
*.log
.idea/
.vscode/
.taste/workflow-runs/

# Per-machine state — never committed
**/.claudemax/
**/.no-amnesia/
**/memory.sqlite
**/memory.sqlite-*
coverage/
.turbo/

# Vendored sibling repo (cloned by setup.sh; sync via pnpm dark-patterns:sync / searchoclock:sync)
vendor/

# searchoclock hook state dir (last-error.md + state.json) — never committed
.searchoclock/
